# 凭据配置文件（模板）
# 实际使用时，复制为 credentials.yaml 并填写实际值，或使用环境变量

github:
  username: "${GH_USERNAME}"
  password: "${GH_PASSWORD}"
  session_cookie: "${GH_SESSION}"

github_actions:
  repo_token: "${REPO_TOKEN}"
  repository: "${GITHUB_REPOSITORY}"
